age <- 21
age <- 22
age
# R언어에서 실행은 한 줄 씩 된다. 파이썬과 다른점.
# 변수 값은 가장 마지막에 실행한 값으로 저장된다.
# 가장 아래줄 X
print(age)
# <- 이 모양은 변수에 값 할당할 때
# = 이 모양은 함수에서 씀

# 자료형
# 숫자형(Numeric) -> 연산 O
# - 정수(integer), 실수(float), numeric: 정수&실수
3.5e+10
# 문자형(character) -> 연산 X
# ""
"Hello World"
# 논리형(Logical) 1: 참/ 0: 거짓
# 1 = True(T) / 0 = False(F)
T
class(T + T)

# 자료형을 확인하는 함수
# class
class("asd")
