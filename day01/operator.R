# 연산자
# 0. 최우선 연산자: ()먼저 연산
# 1. 산술 연산자: 사칙연산(+, -, *, /, ^(e = 10지수), %/%(몫), %%(나머지))
# 2. 비교 연산자(관계 연산자): ==, !=, >, <, <=, >=
num1 <- 4
num2 <- 5
print(num1 == num2)
# 3. 논리 연산자: &(and)(논리곱), |(or)(합연산), !논리값
as.logical(T + T)
as.numeric(T == T)
# 4. 대입 연산자



